//
   Created by Sunny on 8/8/14.

extends ../layout
block content
    .container
        .row
            .col-md-6.col-lg-6
                table.table.table-hover
                    tr
                        td Company
                        td #{position.name}
                    tr
                        td Title
                        td #{position.title}
                    tr
                        td Location
                        td #{position.place.city}, #{position.place.state}, #{position.place.country}
                    tr
                        td Apply Time
                        td #{position.applyDate}
                    tr
                        td Update Time
                        td #{position.updateDate}
                    tr
                        td Status
                        td #{position.result}
            .col-md-6.col-lg-6
                p #{position.requirement}

        a(href="../../#{userId}/list").btn.btn-lg.btn-primary back
